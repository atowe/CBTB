<h1>Listing books</h1>

<table>
  <tr>
    <th>Timelogged</th>
    <th>Isbn</th>
    <th>Condition</th>
    <th>Status</th>
    <th>Title</th>
    <th>Location</th>
    <th>Weight</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= book.timeLogged %></td>
    <td><%= book.isbn %></td>
    <td><%= book.condition %></td>
    <td><%= book.status %></td>
    <td><%= book.title %></td>
    <td><%= book.location %></td>
    <td><%= book.weight %></td>
    <td><%= link_to 'Show', book %></td>
    <td><%= link_to 'Edit', edit_book_path(book) %></td>
    <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book', new_book_path %>
